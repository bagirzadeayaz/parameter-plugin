# Initial setup prompt

Use the prompt in the repository root file `KONTAKT_PARAMETER_INITIAL_PROMPT.md`. It installs the local plugin with automatic runtime updates. No Parameter sign-in or API key is required. Future searches are submitted anonymously to the web platform; the plugin has no direct database access. Start a new task once after installation; compatible future updates are picked up before searches in the same task.
